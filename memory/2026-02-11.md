# 2026-02-11

## Memórias duráveis (operacional)

- Galvão reforçou que havia erro recorrente de contexto em envios; foi adotada regra rígida de envio:
  - Só enviar quando ele usar formato explícito:
    - `ENVIAR PARA +55 ...: texto`
    - ou `ENVIAR PARA [Nome cadastrado]: texto`
- Também houve comando de suspensão total em vários momentos:
  - “suspenda tudo / não fale com ninguém” => responder em silêncio (NO_REPLY) para contatos externos.
- Regra operacional posterior de Galvão:
  - “Só emita as postagens de cursos para os grupos de síndicos.”
- Pedido de backup:
  - Solicitou backup de contatos e grupos em CSV no servidor atual.
  - Arquivos gerados/reportados:
    - `/root/.openclaw/workspace/whatsapp_peers.json`
    - `/root/.openclaw/workspace/whatsapp_groups.json`
  - Diretório retornou vazio (sem contatos/grupos listados via comando usado).
- Tentativa de reset total do OpenClaw foi solicitada, mas checagem posterior mostrou que `/root/.openclaw` e `workspace` ainda existem (não zerou tudo).
- Pedido atual pendente:
  - Galvão perguntou se “foi zerado tudo?” e recebeu que não; pendente confirmação se quer reset real (só workspace vs tudo em `/root/.openclaw`).
